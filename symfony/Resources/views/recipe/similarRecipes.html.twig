<div class="row">
    <div class="col-lg-1"></div>
    <div id="drinks" class="col-lg-10">
        <h4>Podobne przepisy</h4>
        {% for recipe in results.items %}
            {{ include(':search/widget:recipeSearchResultPreview.html.twig', {recipe: recipe}) }}
        {% endfor %}
    </div>
    <div class="col-lg-1"></div>
</div>