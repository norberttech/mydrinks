{{
    'recipe.step.addIngredientIntoShaker.description'|trans(
        {
            "%name%" : step.ingredientName|transchoice(step.amount.value, {}, 'supplies'),
            "%amount%": step.amount.value
        }
    )
}}